# CERTIFICADO TABLET

Para atualizar o tablet via MDM, precisamos sempre gerar o APK com o mesmo certificado.

# Instalação

1. Abrir o diretório "%userprofile%\\.android"
2. Fazer backups dos certificados
3. Substituir os certificados "debug.keystore" e "debug.keystore.lock"
